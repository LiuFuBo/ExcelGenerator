name: Build Windows EXE

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install PySide6 openpyxl pypinyin pyinstaller

      - name: Build EXE
        shell: cmd
        run: pyinstaller --onefile --windowed --name "ExcelGenerator" --icon=header.ico --hidden-import=pypinyin --hidden-import=openpyxl --hidden-import=openpyxl.cell._writer --add-data="jj_header_rounded.png;." --collect-data=pypinyin --noconfirm app.py

      - name: Upload EXE artifact
        uses: actions/upload-artifact@v4
        with:
          name: ExcelGenerator-Windows
          path: dist\ExcelGenerator.exe